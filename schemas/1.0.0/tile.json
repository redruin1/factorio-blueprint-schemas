{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "1.0.0/tile.json",
    "title": "Tile",
    "description": "A schema which contains the keys that all tiles share.",
    "type": "object",
    "properties": {
        "name": {
            "title": "Tile Name",
            "description": "Name of the tile. Must be a valid Factorio tile.",
            "type": "string"
        },
        "position": {
            "title": "Tile Position",
            "description": "Integer grid cell that this tile occupies.",
            "$ref": "../common/position.json"
        }
    },
    "required": ["name", "position"]
}
